{
  "name": "mymx",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "homepage": "https://mymx.sharapov.biz",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sharapov-outsource/mymx.git"
  },
  "bugs": "https://github.com/sharapov-outsource/mymx/issues",
  "description": "SPF expanded and counted against the lookup limit, DKIM, DMARC with external report authorisation, MTA-STS, DANE, STARTTLS and a read-only open-relay probe",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "scan": "node scripts/scan-cli.js",
    "icons": "node scripts/make-icons.js",
    "check:i18n": "node scripts/check-i18n.js",
    "check:syntax": "node --check public/app.js && node --check public/i18n.js && node --check server/index.js && node --check server/scan.js && node --check server/session.js && node --check server/smtp.js && node --check server/mx.js && node --check server/spf.js && node --check server/dkim.js && node --check server/dmarc.js && node --check server/mtasts.js && node --check server/dane.js && node --check server/starttls.js && node --check server/rdns.js && node --check server/grade.js",
    "test:unit": "node --test \"test/*.test.js\"",
    "smoke": "node scripts/smoke.js",
    "test": "npm run check:syntax && npm run check:i18n && npm run test:unit && npm run smoke"
  },
  "dependencies": {
    "@fastify/rate-limit": "^10.2.2",
    "@fastify/static": "^10.1.2",
    "@sharapov/dns-wire": "git+https://github.com/sharapov-outsource/dns-wire.git#v1.0.0",
    "@sharapov/service-kit": "git+https://github.com/sharapov-outsource/service-kit.git#v1.4.1",
    "fastify": "^5.2.1",
    "yaml": "^2.7.0"
  }
}
